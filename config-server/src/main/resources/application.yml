server:
  port: 8888

spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Leilorr/config-repo
          clone-on-start: true